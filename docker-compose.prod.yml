services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.prod
    image: formbrew-backend-prod
    ports:
      - "8000:8000"
    env_file: .env.prod
    environment:
      ENVIRONMENT: prod
